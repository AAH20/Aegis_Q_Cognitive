# Aegis Quantum-Cognitive — runtime requirements.
# Pinned to the loose minimum versions verified by the alpha demo;
# tighten before flying this in any classified network.
click>=8.1
rich>=13.7
networkx>=3.2
scapy>=2.5
cryptography>=42.0
# Optional: liboqs-python provides ML-KEM-768 / ML-DSA-65 / SLH-DSA
# bindings. Requires the system `liboqs` library. Install via:
#   pip install "aegis-quantum-cognitive[pqc]"
# liboqs-python>=0.10.0
# Optional: weasyprint converts the FDA / NSM-10 compliance Markdown
# into PDF. The Markdown is the regulatory artifact; the PDF is the
# convenience export. `pandoc` (system binary) is also required for
# the HTML intermediate. Install via:
#   pip install "aegis-quantum-cognitive[render]"
#   brew install pandoc            # or apt-get install pandoc
# weasyprint>=62
